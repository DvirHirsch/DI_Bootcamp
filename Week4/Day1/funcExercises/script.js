// Exercise 1
let myAge = function (age) {
	console.log(
		`My mum(${age * 2}), is twice my age(${age}), and my dad(${
			age * 2 * 1.2
		}), is 1.2 the age of my mum.`
	);
};

myAge(15);

// Exercise 2
let myAge1 = function (age) {
	return console.log(`My mum(${age * 2}), is twice my age(${age}).`);
};

myAge1(20);
